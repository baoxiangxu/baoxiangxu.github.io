# 编辑指南

打开文件夹你只会看到 8 样东西,每一样都有明确用途 —— 以前根目录那 17 个
`index.html`、`favicon`、`resume.pdf`… 都是构建自动生成的副本,现在不再出现了
(见文末「为什么根目录干净了」)。

```
originals/         所有图片素材      ← 加照片、换头像、换论文配图，都放这
public/            静态文件          ← 换 CV、换下载包，放这
src/               网站代码          ← 只有 src/content/ 需要你改
scripts/           工具脚本
本地预览.command    双击本地预览
编辑指南.md         本文件
README.md          英文说明（给访客看的）
package.json       工程配置
```

**一条规则记住就行:**

> **`originals/` 是你放素材的地方,`public/images/` 是脚本生成的成品 —— 后者别手动改。**

跑一次 `npm run images`,脚本会从 `originals/` 读原件,自动生成网站要用的
各种尺寸放进 `public/images/`。

---

## 你要改的东西,只有两个地方

### ① `src/content/` —— 所有文字、链接、论文

六个 JSON 文件,网站上每一个字都在里面。改完存盘即可,不需要懂代码。

| 文件 | 管什么 |
| --- | --- |
| `profile.json` | 姓名、首页那句话、侧边栏头衔、About 三段简介、邮箱/LinkedIn/GitHub/CV 链接、SEO 关键词 |
| `education.json` | 学历时间线(学校、年份、学位) |
| `publications.json` | 论文(标题、合作者、期刊、年份、链接、摘要、配图、标签) |
| `projects.json` | 项目卡片(标题、说明、下载链接、配图、标签) |
| `galleries.json` | Food / Travel 相册的标题、说明文字、照片清单 |
| `theme.json` | 四个主色 |

### ② `public/` —— 要整个替换的文件

| 位置 | 放什么 |
| --- | --- |
| **`public/files/`** | **CV(`resume.pdf`)、可下载的压缩包** ← 换 CV 直接覆盖这个文件,链接不用改 |
| `public/index.html` | 网页标题、搜索引擎描述、给 Google 学术看的结构化数据 |
| `public/` 根部 | 浏览器图标、`robots.txt`、`sitemap.xml`、分享预览图 —— 必须放在最外层,一般不用动 |
| ~~`public/images/`~~ | **全部由脚本生成,不要手动放东西进去** |

### ③ `originals/` —— 所有图片素材

| 位置 | 放什么 |
| --- | --- |
| `originals/avatar.jpg` | 你的头像原件 |
| `originals/photos/food/` | 美食照原图,命名 `food-01.jpg`、`food-02.jpg`… |
| `originals/photos/travel/` | 旅行照原图,命名 `travel-01.jpg`… |
| `originals/figures/` | 论文和项目的配图原件(`paper1.jpg`、`python.png` 等) |

放好之后跑 `npm run images`,自动生成:

| 生成到 | 是什么 |
| --- | --- |
| `public/images/avatar-360.webp` | 侧边栏头像 |
| `public/images/food/thumbs/` | 640px,相册网格里显示的 |
| `public/images/food/large/` | 2000px,点开大图时才下载 |
| `public/images/cards/` | 520px,论文/项目卡片右边那张小图 |
| `public/images/figures/` | 1400px,点论文标题后弹窗里的大图 |

其余目录(`src/components/`、`src/app/`、`scripts/`、`node_modules/`、`build/`)
都是框架和工具,除非要改功能,否则不用碰。

---

## 常用操作

### 改文字 / 加论文

改 `src/content/` 里对应的 json,然后:

```bash
git add -A && git commit -m "update content" && git push
```

推送后 GitHub 会**自动构建并发布**,约一两分钟生效。不需要你手动构建。

### 加一张生活照

1. 照片放进 `originals/photos/food/` 或 `originals/photos/travel/`,
   命名接着现有编号,例如 `food-18.jpg`
2. 生成网站要用的尺寸:
   ```bash
   npm run images
   ```
3. 把 `"food-18"` 加进 `src/content/galleries.json` 对应相册的 `photos` 列表
4. commit + push

### 换 CV

用新 PDF 覆盖 `public/files/resume.pdf`,然后 commit + push。

### 换头像 / 换论文配图

用新图覆盖 `originals/avatar.jpg` 或 `originals/figures/paper1.jpg`,
跑 `npm run images`,然后 commit + push。文件名不变的话,内容文件不用动。

### 本地看效果

```bash
npm run build      # 构建
```

然后双击预览脚本:

| 系统 | 双击这个 |
| --- | --- |
| **Mac** | `本地预览.command` |
| **Windows** | `本地预览.bat` |

(另一个文件在你的系统上双击没反应,这是正常的 —— 两个都留着,换电脑时不用改。
也可以在终端里跑 `npm run preview`,效果一样。)

窗口里会给出两个地址:本机的 `localhost`,和手机可以打开的局域网地址
(手机需连同一个 Wi-Fi)。

---

## 在 Windows 上使用

这套文件在 Windows / Mac / Linux 上都能跑,只是准备工作略有不同。

**需要先装两样东西:**

| 装什么 | 哪里下 | 什么时候需要 |
| --- | --- | --- |
| **Node.js**(选 LTS 版) | <https://nodejs.org> | 必装。构建网站、本地预览都靠它 |
| **Python 3** | <https://www.python.org> | 只有要处理图片(`npm run images`)时才需要 |

装 Python 时记得勾选 **"Add Python to PATH"**,然后装图片库:

```
py -3 -m pip install pillow
```

**之后的命令和 Mac 完全一样**,在 PowerShell 或命令提示符里:

```
cd 到项目文件夹
npm install        # 只需第一次
npm run build
npm run images     # 只有加了新照片才需要
```

`npm run images` 会自己找到你机器上的 Python(Windows 上通常是 `py` 或 `python`,
不是 `python3`),不用你手动改命令。

**预览**:双击 **`本地预览.bat`**。首次运行 Windows 防火墙会弹窗问是否允许,
点「允许访问」,手机才能连上。

**注意**:`本地预览.command` 是 Mac 专用的,Windows 上双击它没反应,这是正常的
—— 两个文件都留着,你在哪个系统上就用哪个。预览本身只需要 Node.js,不需要 Python。

---

## 首次设置(只做一次)

这个文件夹还没连上 GitHub。第一次推送时:

```bash
git init
git add -A
git commit -m "rebuild site"
git branch -M main
git remote add origin https://github.com/baoxiangxu/baoxiangxu.github.io.git
git push -f origin main
```

然后到 GitHub 仓库页面:
**Settings → Pages → Build and deployment → Source** 选 **「GitHub Actions」**

设置完成后,以后每次 push 都会自动构建发布。

---

## 为什么根目录干净了

GitHub Pages 有两种工作方式:

- **旧方式**:你自己构建,把生成的 `index.html`、`static/`、`images/`、各种图标
  全部提交到仓库根目录 —— 这就是之前那 17 个「散落文件」的来源。它们看着像可以改,
  其实改了下次构建就被覆盖。
- **现在的方式**:仓库里只放源文件,GitHub 收到 push 后自己跑构建、自己发布
  (配置在 `.github/workflows/deploy.yml`)。

好处是根目录只剩源文件、仓库体积小很多、也不会再有「改了没用的文件」。

---

## 关于图片的几个说明

**为什么每张生活照有两个尺寸?**
`thumbs/`(640px,约 55 KB)是网格里显示用的;`large/`(2000px,约 244 KB)只在点开大图时才下载。
网格若直接用大图,打开首页要多下载 43 倍数据。

**为什么只有 `.webp` 没有 `.jpg`?**
WebP 体积只有 JPEG 的 40%,现代浏览器全支持(Safari 从 2020 年起)。
之前每张存两种格式,多占 29 MB、多 68 个文件,只服务不到 3% 的老设备。
需要恢复兜底的话,把 `scripts/optimize_images.py` 里的 `JPEG_FALLBACK` 改成 `True`,
重跑 `npm run images`。

**为什么原图放在 `originals/` 而不是 `public/`?**
`public/` 里的东西会被原样复制进每次构建。原图有 115 MB,而访客永远不会下载它们
(网站只加载脚本生成的小尺寸版本),放在 `public/` 外面能让部署体积从 172 MB 降到 15 MB。

**同一张照片为什么在两个地方都能看到?**
`originals/photos/food/food-01.jpg` 是相机原图(4032×3024,4 MB),没有人会下载它;
`public/images/food/thumbs/food-01.webp`(640px,55 KB)和 `large/food-01.webp`(2000px,244 KB)
是脚本从它生成的网页版本,访客下载的是这两个。
就像 Word 原稿和导出的 PDF —— 同一份内容,一个是原料,一个是成品。
